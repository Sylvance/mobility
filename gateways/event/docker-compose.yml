version: '3.9'
services:
  kafka:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        runner: "docker"
    ports:
      - '2181:2181'
      - '9092:9092'
    volumes:
      - .:/event-gateway-kafka